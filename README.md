# РАСТИ В ИТ

Система оценки и развития персонала в IT-компаниях.

## Описание проекта

**РАСТИ В ИТ** - это полнофункциональная система для управления оценкой эффективности сотрудников, включающая:

- 🎯 Управление целями и задачами
- 📝 Самооценку сотрудников
- 👥 Оценку 360 градусов
- ⭐ Оценку менеджеров
- 📊 Оценку потенциала сотрудников
- 📈 9-Box матрицу талантов
- 📄 Итоговые отчеты и рекомендации по salary increase

## Технологический стек

### Backend
- Python 3.11
- Django 5.2
- Django REST Framework
- PostgreSQL 15
- JWT Authentication
- Docker & Docker Compose

### Frontend
- React 18
- React Router DOM
- Axios
- Vite
- Docker & Nginx

## Структура проекта

```
rasti-v-it/
├── Backend/                # Django бэкенд
│   ├── api/               # API приложение
│   ├── Backend/           # Настройки Django
│   ├── Dockerfile         # Docker для бэкенда
│   ├── docker-compose.yml # Compose для бэкенда с БД
│   ├── requirements.txt   # Python зависимости
│   └── entrypoint.sh      # Скрипт запуска
│
├── Frontend/              # React фронтенд
│   ├── src/
│   │   ├── api/          # API клиент
│   │   ├── components/   # React компоненты
│   │   ├── App.jsx       # Главный компонент
│   │   └── main.jsx      # Точка входа
│   ├── Dockerfile        # Docker для фронтенда
│   ├── nginx.conf        # Конфигурация Nginx
│   └── package.json      # NPM зависимости
│
└── docker-compose.yml     # Полный compose для всей системы
```

## Быстрый старт

### Вариант 1: Запуск всей системы через Docker Compose (рекомендуется)

```bash
# Клонировать репозиторий
git clone <repository-url>
cd rasti-v-it

# Запустить всю систему (БД + Backend + Frontend)
docker-compose up --build

# Приложение будет доступно:
# - Frontend: http://localhost
# - Backend API: http://localhost:8000
# - Admin панель: http://localhost:8000/admin
```

### Вариант 2: Запуск только бэкенда с БД

```bash
cd Backend

# Создать файл .env из примера
copy .env.example .env

# Запустить бэкенд и PostgreSQL
docker-compose up --build

# API будет доступен на http://localhost:8000
```

### Вариант 3: Локальная разработка

#### Backend

```bash
cd Backend

# Создать виртуальное окружение
python -m venv venv
venv\Scripts\activate  # На Windows
# source venv/bin/activate  # На Linux/Mac

# Установить зависимости
pip install -r requirements.txt

# Создать .env файл и настроить переменные
copy .env.example .env

# Применить миграции
python manage.py migrate

# Создать суперпользователя
python manage.py createsuperuser

# Запустить сервер
python manage.py runserver
```

#### Frontend

```bash
cd Frontend

# Установить зависимости
npm install

# Создать .env файл
copy .env.example .env

# Запустить dev сервер
npm run dev

# Приложение будет доступно на http://localhost:5173
```

## API Endpoints

### Authentication
- `POST /api/auth/register/` - Регистрация
- `POST /api/auth/login/` - Вход
- `POST /api/auth/logout/` - Выход
- `GET /api/auth/me/` - Текущий пользователь
- `POST /api/auth/token/refresh/` - Обновление токена

### Resources
- `/api/departments/` - Отделы
- `/api/employees/` - Сотрудники
- `/api/goals/` - Цели
- `/api/tasks/` - Задачи
- `/api/self-assessments/` - Самооценки
- `/api/feedback-360/` - Оценки 360
- `/api/manager-reviews/` - Оценки менеджеров
- `/api/potential-assessments/` - Оценка потенциала
- `/api/final-reviews/` - Итоговые отчеты

## Переменные окружения

### Backend (.env)
```env
DEBUG=False
SECRET_KEY=your-secret-key-here
ALLOWED_HOSTS=localhost,127.0.0.1
DB_NAME=rasti_db
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=db
DB_PORT=5432
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
```

### Frontend (.env)
```env
VITE_API_URL=http://localhost:8000
```

## Учетные данные по умолчанию

После запуска через Docker будет создан суперпользователь:
- **Username:** admin
- **Password:** admin

⚠️ **Важно:** Измените пароль при первом входе!

## Сборка для продакшена

### Backend
```bash
cd Backend
docker build -t rasti-backend .
docker run -p 8000:8000 --env-file .env rasti-backend
```

### Frontend
```bash
cd Frontend
docker build -t rasti-frontend --build-arg VITE_API_URL=http://your-api-url .
docker run -p 80:80 rasti-frontend
```

### Полная система
```bash
docker-compose -f docker-compose.yml up --build -d
```

## Особенности реализации

### Backend
- JWT аутентификация с автоматическим refresh
- Автоматический расчет баллов при создании оценок
- Фильтрация и пагинация для всех списков
- Права доступа: менеджеры видят свой отдел, сотрудники - только свои данные
- 9-Box матрица для визуализации потенциала
- Подробная админ-панель для управления

### Frontend
- Single Page Application на React
- Защищенные роуты с проверкой JWT
- Автоматическое обновление токенов
- Адаптивный дизайн
- Разделение функционала для менеджеров и сотрудников
- Визуализация 9-Box матрицы

## Лицензия

MIT

## Авторы

Разработано для системы оценки персонала IT-компаний